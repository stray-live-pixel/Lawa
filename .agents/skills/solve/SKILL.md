---
name: solve
description: "Реализовать задачу flows-2 в отдельной Git-ветке: уточнить результат, выбрать законченный объём до примерно 500 строк, написать и проверить код. Использовать для /solve, $solve или просьбы реализовать доработку. Не публикует PR и не выполняет merge."
---

# Solve: задача → ветка → проверенная реализация

## Договор с пользователем

Возьми задачу из аргумента или текущего обсуждения. Явный вызов `/solve` или `$solve`
означает реализацию в отдельной ветке с проверками, но не коммит, пуш или слияние.
Если пользователь отдельно попросил публикацию, учитывай это разрешение;
иначе оставь изменения для последующего вызова `commit`.

Пример: `$solve Добавь проверку циклов в workflow`.

## До изменений

1. Прочитай корневой `AGENTS.md` и применимые вложенные инструкции. Кратко зафиксируй
   результат и критерии готовности. Уточняй только то, без чего нельзя безопасно продолжить.
2. Проверь корень репозитория, `git status --short --branch`, staged и unstaged diff,
   untracked-файлы, `git remote -v` и `git worktree list`. В flows-2 базовая ветка —
   `main`, remote — `origin`; если фактическая конфигурация отличается, не меняй её молча.
3. Выдели законченную часть, готовую к production, в пределах примерно 500 строк
   изменённого кода на весь будущий PR. Большую задачу предложи разбить по работающим
   результатам; не выбирай за пользователя существенно урезанный результат.
   Не экономь на тестах и комментариях. Если просят обойти лимит, объясни сложность
   ревью большого изменения и предложи конкретное разбиение.

## Ветка

- Для новой задачи получи свежие ссылки через `git fetch origin`. При чистой рабочей
  папке создай `codex/<короткое-имя-задачи>` от `origin/main`, например командой
  `git switch --no-track -c codex/<имя> origin/main`. Не коммить в `main`.
- Если текущая ветка уже относится к этой задаче и её PR ещё не слит или закрыт,
  продолжай её. Не создавай вторую ветку при повторном вызове.
- Ветка другой задачи или уже слитого PR не является основой новой доработки.
  Не включай чужие коммиты в новый PR. Проверь принадлежность ветки по истории,
  diff и, если доступен GitHub CLI, связанному PR.
- При незакоммиченных изменениях сначала установи их принадлежность. Изменения этой
  задачи сохраняй; если они находятся на `main`, создай отдельную ветку от текущего
  HEAD без потери изменений и проверь её отношение к свежему `origin/main`.
  При смешанных или неясных изменениях остановись с конкретным вопросом.
- Не делай автоматический stash, reset, очистку файлов, rebase опубликованной ветки
  или force-push. Если нужная ветка занята другим worktree, не переключай тот worktree
  и не обходи защиту: сообщи, где она используется.

## Реализация и проверки

- Меняй только необходимое для задачи. Подробно поясняй устройство и причины решений,
  ограничения и важные инварианты согласно `AGENTS.md`.
- Найди команды проверки в самом проекте и его CI. Запусти подходящие тесты, сборку
  и статические проверки. Для изменения только документации достаточно проверки
  содержания, ссылок и форматирования; не выдумывай отсутствующий тестовый контур.
- Перед завершением просмотри весь будущий PR относительно merge-base с `origin/main`,
  включая уже сделанные коммиты, текущие изменения и новые файлы. Размер — не только
  последний коммит: оцени добавленные и удалённые строки кода, а также общий объём ревью.
- Убедись, что результат работает без следующего PR. Не оставляй обязательную обработку
  ошибок, тесты или восстановление работоспособности на потом.
- Если проверка не прошла, исправь причины в рамках задачи. Внешнюю блокировку или
  требующее расширения задачи исправление опиши пользователю; не объявляй результат готовым.

## Завершение

Коротко сообщи результат, ветку, выполненные проверки и оставшиеся ограничения.
Укажи готовность к `commit`; без отдельного запроса не публикуй изменения.
